import 'package:flutter/material.dart';
import 'package:adobe_xd/pinned.dart';

class LightSlider2DiscreteAknob4States extends StatelessWidget {
  LightSlider2DiscreteAknob4States({
    Key? key,
  }) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Stack(
      children: <Widget>[
        Container(),
        Container(),
        Container(),
      ],
    );
  }
}
