import 'package:flutter/material.dart';
import 'package:adobe_xd/pinned.dart';

class LightSlider2DiscreteBValueIndicator3States extends StatelessWidget {
  LightSlider2DiscreteBValueIndicator3States({
    Key? key,
  }) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Stack(
      children: <Widget>[
        Container(),
        Container(),
        Container(),
      ],
    );
  }
}
