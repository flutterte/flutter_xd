import 'package:flutter/material.dart';
import 'package:adobe_xd/pinned.dart';

class LightSlider1ContinuousAknob4States extends StatelessWidget {
  LightSlider1ContinuousAknob4States({
    Key? key,
  }) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Stack(
      children: <Widget>[
        Container(),
        Container(),
        Container(),
      ],
    );
  }
}
